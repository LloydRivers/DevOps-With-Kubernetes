apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hapi-server-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hapi-server-svc
                port:
                  number: 2345
